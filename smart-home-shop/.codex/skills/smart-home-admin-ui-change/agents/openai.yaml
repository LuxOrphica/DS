interface:
  display_name: "Smart Home Admin UI Change"
  short_description: "Admin UI changes with sanity checks."
  default_prompt: "Use this when changing admin pages, admin scripts, admin styles, or admin API flows in smart-home-shop."
